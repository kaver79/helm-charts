apiVersion: v1
kind: Service
metadata:
  name: {{ template "cals-api.fullname" . }}
  labels:
    app: {{ template "cals-api.name" . }}
    chart: {{ template "cals-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "cals-api.name" . }}
    release: {{ .Release.Name }}
